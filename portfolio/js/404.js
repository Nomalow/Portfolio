/**
 * 404.js — Page d'erreur
 * Pas de logique particulière, les animations sont en CSS pur.
 */
document.addEventListener('DOMContentLoaded', () => {
  // Rien à faire, les animations hero sont gérées via CSS (animation: fadeUp)
});
